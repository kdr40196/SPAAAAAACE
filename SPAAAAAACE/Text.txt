enemy colliging with each other

AI
	maybe - add enemy states - IDLE, PLAYER_DETECTED, CHASING, RETURNING
	add original position, angle so enemies can return to IDLE state
	improve Enemy::move()


more accurate colliders
	per pixel collision and collider rotation

make infinite

scaling

multiplayer, if possible